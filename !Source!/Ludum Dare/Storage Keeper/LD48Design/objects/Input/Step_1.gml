up =  keyboard_check(ord("W"));
down = keyboard_check(ord("S"));
left = keyboard_check(ord("A"));
right = keyboard_check(ord("D"));

grab = mouse_check_button_pressed(mb_left);

jump = keyboard_check_pressed(ord(" "));